{% extends 'baseAdmin.html.twig' %}

{% block title %}Editer l'utilisateur{% endblock %}

{% block body %}
<div class="container">
    <h1>Editer {{utilisateur.nom}} {{utilisateur.prenom}}</h1>
    {{ form_start(form) }}

        {{ form_widget(form) }}
        <button class="btn btn-warning">{{ button_label|default('Valider') }}</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_utilisateurs') }}">Retour</a>
</div>
{% endblock %}
