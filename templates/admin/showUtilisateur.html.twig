{% extends 'baseAdmin.html.twig' %}

{% block title %}{{utilisateur.nom}}{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-9">
        <h1  style="text-align: center; margin-top: 10px;  margin-left: 250px;  margin-bottom: 30px;">{{ utilisateur.nom }} {{ utilisateur.prenom }}</h1>
        </div>
       
    </div>
    <form >
    <div class="form-group row">
            <label class="col-sm-2 col-form-label"> id </label> 
            <div class="col-sm-10">
                 <input type="text" readonly class="form-control-plaintext" value="{{ utilisateur.id }}" >
            </div>
            <label class="col-sm-2 col-form-label"> Email </label> 
            <div class="col-sm-10">
                 <input type="text" readonly class="form-control-plaintext" value="{{ utilisateur.email }}" >
            </div>
    </div>
    </form>
            
        
    <a href="{{ path('admin_categories') }}">Retour à la liste</a>


</div>
{% endblock %}
